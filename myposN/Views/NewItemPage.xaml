<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MyPOS.Views.NewItemPage"
             Title="New Item">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="CancelItem_Clicked" />
        <ToolbarItem Text="Save" Clicked="SaveItem_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Entry Text="{Binding Item.Text}" d:Text="Item name" FontSize="Small" />
            <Entry Text="{Binding Item.Category}" d:Text="Category" FontSize="Small" Margin="0" />
            <Entry Text="{Binding Item.Price}" d:Text="Price" FontSize="Small" Margin="0" />
            <Entry Text="{Binding Item.Cost}" d:Text="Cost" FontSize="Small" Margin="0" />
            <Entry Text="{Binding Item.SKU}" d:Text="SKU" FontSize="Small" Margin="0" />
            <Entry Text="{Binding Item.Barcode}" d:Text="Barcode" FontSize="Small" Margin="0" />
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>
